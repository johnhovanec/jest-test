function testSum (a, b) {
    const sum = a + b
    return sum;
}

function testDifference (a, b) {
    const diff = a - b
    return diff;
}

const utils = {
    testSum,
    testDifference,
}

export default utils;